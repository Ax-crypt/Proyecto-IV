server:
  port: 8082

spring:
  application:
    name: ProyectoFinalDiscos

  # Configuración de la base de datos MySQL
  datasource:
    url: jdbc:mysql://localhost:3306/proyectofinaldawii
    username: ----
    password: ----

  # Configuración de JPA
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


